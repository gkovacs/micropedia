// Generated by LiveScript 1.3.1
(function(){
  var insertArticle, out$ = typeof exports != 'undefined' && exports || this;
  out$.insertArticle = insertArticle = function(article_name){
    var havearticle;
    havearticle = function(data){
      var ndata, i$, ref$, len$, x, target_article, newdiv;
      if (data.indexOf('article does not exist:') === 0) {
        toastr.error('no such article: ' + article_name);
        return;
      }
      ndata = $(data);
      for (i$ = 0, len$ = (ref$ = ndata.find('a')).length; i$ < len$; ++i$) {
        x = ref$[i$];
        target_article = $(x).attr('href');
        $(x).attr('href', '#').attr('onclick', "insertArticle('" + target_article + "')");
      }
      newdiv = $('<div>').css({
        'padding': '10px',
        'margin': '10px',
        'border-color': 'black',
        'border-style': 'solid',
        'border-width': '1px',
        'border-radius': '10px'
      }).html(ndata);
      return $('#feeditems').prepend(newdiv);
    };
    return $.get("/w/" + article_name, havearticle).fail(function(){
      toastr.error('no such article: ' + article_name);
      return false;
    });
  };
  $(document).ready(function(){
    return insertArticle('index');
  });
}).call(this);
