// Generated by LiveScript 1.3.1
(function(){
  $(document).ready(function(){
    var checkcode;
    checkcode = function(){
      var usercode, fibcode, livescript, jscode, e, results;
      usercode = $('#inputcode').val();
      fibcode = $('#fibcode').text().split('Check').join(usercode);
      livescript = require('LiveScript');
      try {
        jscode = livescript.compile(fibcode);
      } catch (e$) {
        e = e$;
        toastr.error(e.message);
        return;
      }
      try {
        results = eval(jscode);
      } catch (e$) {
        e = e$;
        toastr.error(e.message);
        return;
      }
      return toastr.success('Yay your answer is correct. You win 1 internetz');
    };
    $('#checkbutton').click(function(){
      return checkcode();
    });
    return $('#inputcode').keydown(function(evt){
      var code, ref$;
      code = (ref$ = evt.keyCode) != null
        ? ref$
        : evt.which;
      if (code === 13) {
        evt.preventDefault();
        return checkcode();
      }
    });
  });
}).call(this);
